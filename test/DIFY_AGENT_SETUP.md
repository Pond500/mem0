# 🤖 วิธีตั้งค่า Agent ใน Dify พร้อม Memory Tools

## 📋 ขั้นตอนการตั้งค่า

### 1. สร้าง Agent Application ใน Dify
1. ไปที่ Dify Dashboard
2. คลิก **Create Application** → เลือก **Agent**
3. ตั้งชื่อ Agent (เช่น "Memory Assistant")

### 2. เพิ่ม Custom Tools
1. ไปที่ **Tools** section
2. คลิก **Add Tool** → **Custom Tool**
3. ✅ Import `openapi.json` ที่คุณได้เตรียมไว้แล้ว
4. ควรจะมี 3 tools:
   - `addMemory` - บันทึกความจำ
   - `searchMemories` - ค้นหาความจำ
   - `getAllMemories` - ดูความจำทั้งหมด

### 3. ใส่ INSTRUCTIONS (สำคัญมาก!)

คัดลอก instructions ด้านล่างนี้ไปใส่ใน **Agent Instructions** box:

---

## 📝 Instructions สำหรับ Dify Agent (ภาษาไทย)

```
คุณคือ AI Assistant ที่มีความสามารถในการจดจำข้อมูลของผู้ใช้ โดยใช้ระบบ Memory Management

🧠 การใช้ Memory Tools:

1. **searchMemories** - ใช้ก่อนตอบคำถาม
   ✅ เมื่อ: ผู้ใช้ถามเกี่ยวกับข้อมูลที่อาจเคยบอกไว้
   📌 วิธี: ค้นหาด้วยคีย์เวิร์ดที่เกี่ยวข้อง
   ⚠️ ถ้าไม่เจอ: บอกผู้ใช้ตรงๆ ว่าไม่มีข้อมูลนี้ในความจำ

2. **addMemory** - บันทึกข้อมูลสำคัญ
   ✅ บันทึกเมื่อผู้ใช้:
      - แนะนำตัว (ชื่อ, อายุ, อาชีพ, ที่อยู่)
      - บอกความชอบ/ไม่ชอบ
      - บอกเป้าหมาย/แผนการ
      - บอกข้อมูลส่วนตัวที่สำคัญ
   ⚠️ ไม่ต้องบันทึก:
      - คำถามทั่วไป
      - การทักทาย
      - ข้อมูลที่ไม่เกี่ยวกับผู้ใช้

3. **getAllMemories** - ใช้เมื่อผู้ใช้ถาม
   ✅ เมื่อ: ผู้ใช้ถามว่า "คุณจำอะไรเกี่ยวกับฉันได้บ้าง"
   📌 แสดง: สรุปข้อมูลทั้งหมดในรูปแบบที่อ่านง่าย

🎯 กฎสำคัญ:
- ✅ ตอบเป็นภาษาธรรมชาติ ไม่ใช่ JSON
- ✅ ใช้ข้อมูลจาก memory เพื่อตอบคำถามให้เป็นส่วนตัว
- ✅ บันทึกข้อมูลทันทีเมื่อผู้ใช้บอก
- ✅ ค้นหา memory ก่อนตอบคำถามเกี่ยวกับผู้ใช้
- ⚠️ ถ้าไม่แน่ใจว่ามีข้อมูลหรือไม่ → ค้นหาก่อน
```

---

## 🌍 Instructions สำหรับ Dify Agent (English)

```
You are an AI Assistant with Memory Management capabilities.

🧠 Memory Tools Usage:

1. **searchMemories** - Use before answering questions
   ✅ When: User asks about information they might have shared before
   📌 How: Search with relevant keywords
   ⚠️ If not found: Tell the user directly that you don't have this information

2. **addMemory** - Save important information
   ✅ Save when user:
      - Introduces themselves (name, age, job, location)
      - Shares preferences/dislikes
      - Mentions goals/plans
      - Provides personal information
   ⚠️ Don't save:
      - General questions
      - Greetings
      - Non-personal information

3. **getAllMemories** - Use when user asks
   ✅ When: User asks "What do you remember about me?"
   📌 Show: Summarize all information in readable format

🎯 Important Rules:
- ✅ Respond in natural language, not JSON
- ✅ Use memory data to personalize responses
- ✅ Save information immediately when user shares
- ✅ Search memory before answering user-related questions
- ⚠️ If unsure whether you have the information → search first
```

---

## 🧪 ขั้นตอนการทดสอบ

### Test Case 1: บันทึกข้อมูลโปรไฟล์
**คุณ:** สวัสดีครับ ผมชื่อ จอห์น อายุ 28 ปี เป็นโปรแกรมเมอร์ อยู่กรุงเทพฯ

**คาดหวัง:**
- Agent ใช้ `addMemory` บันทึกข้อมูล
- ตอบกลับแบบเป็นมิตร เช่น "ยินดีที่ได้รู้จักครับคุณจอห์น"

---

### Test Case 2: บันทึกความชอบ
**คุณ:** ผมชอบเขียน Python และ FastAPI นะ แต่ไม่ชอบ syntax ที่ยาวเกินไป

**คาดหวัง:**
- Agent ใช้ `addMemory` บันทึก preferences
- ตอบกลับ เช่น "จดไว้แล้วครับ คุณชอบ Python กับ FastAPI"

---

### Test Case 3: ค้นหาข้อมูล
**คุณ:** ผมชื่ออะไรนะ

**คาดหวัง:**
- Agent ใช้ `searchMemories` ด้วย query "name"
- ตอบกลับ "คุณชื่อจอห์นครับ"

---

### Test Case 4: ค้นหาข้อมูลที่ไม่มี
**คุณ:** ผมเคยบอกสีโปรดมั้ย

**คาดหวัง:**
- Agent ใช้ `searchMemories` ด้วย query "favorite color"
- ตอบกลับ "คุณยังไม่ได้บอกสีโปรดครับ"

---

### Test Case 5: ดูข้อมูลทั้งหมด
**คุณ:** คุณจำอะไรเกี่ยวกับผมได้บ้าง

**คาดหวัง:**
- Agent ใช้ `getAllMemories`
- แสดงสรุปข้อมูลทั้งหมดในรูปแบบที่อ่านง่าย

---

### Test Case 6: การสนทนาต่อเนื่อง
**คุณ:** ช่วยแนะนำ framework ที่เหมาะกับผมหน่อย

**คาดหวัง:**
- Agent ใช้ `searchMemories` หา preferences
- แนะนำ framework โดยอ้างอิงความชอบที่เคยบันทึกไว้ (Python, FastAPI)

---

## ⚙️ การตั้งค่าเพิ่มเติม

### Model Selection
- แนะนำ: **GPT-4** หรือ **Claude 3.5** สำหรับการเลือกใช้ tools ที่แม่นยำ
- ใช้ได้: GPT-3.5-turbo (แต่อาจจะเลือก tool ผิดบ้าง)

### Temperature
- แนะนำ: **0.7** สำหรับความสมดุลระหว่างความแม่นยำและความเป็นธรรมชาติ

### Tool Call Strategy
- เลือก: **Auto** (ให้ Agent เลือกใช้ tool เองตาม instructions)

---

## 🚨 ปัญหาที่พบบ่อยและวิธีแก้

### 1. Agent ไม่ใช้ tools
**สาเหตุ:** Instructions ไม่ชัดเจน
**แก้ไข:** ตรวจสอบว่าใส่ instructions แล้วหรือยัง และเพิ่มตัวอย่างการใช้งาน

### 2. Agent ตอบกลับเป็น JSON
**สาเหตุ:** Instructions ไม่ได้บอกให้ตอบเป็นภาษาธรรมชาติ
**แก้ไข:** เพิ่มบรรทัด "ตอบเป็นภาษาธรรมชาติ ไม่ใช่ JSON" ใน instructions

### 3. Agent บันทึก memory มากเกินไป
**สาเหตุ:** Instructions ไม่ได้บอกว่าอะไรไม่ต้องบันทึก
**แก้ไข:** เพิ่มส่วน "⚠️ ไม่ต้องบันทึก" ให้ชัดเจน

### 4. Agent ไม่ค้นหาก่อนตอบ
**สาเหตุ:** Instructions ไม่เน้นให้ search ก่อน
**แก้ไข:** เพิ่ม "ค้นหา memory ก่อนตอบคำถามเกี่ยวกับผู้ใช้" ใน instructions

---

## 📊 การตรวจสอบ Logs

ใน Dify คุณสามารถดู logs ได้ที่:
1. ไปที่ **Logs & Ann.** tab
2. ดูว่า Agent เลือกใช้ tool อะไรบ้าง
3. ตรวจสอบ input/output ของแต่ละ tool call

---

## 🎯 Tips สำหรับการทดสอบ

1. **ทดสอบทีละ case** - อย่ารีบทดสอบหลาย case พร้อมกัน
2. **ดู logs** - ดูว่า Agent เรียกใช้ tool ถูกต้องหรือไม่
3. **ปรับ instructions** - ถ้า Agent ทำงานไม่ถูกต้อง ลองปรับ instructions
4. **ใช้ user_id ที่แตกต่างกัน** - เพื่อทดสอบ user isolation
5. **ทดสอบ edge cases** - เช่น ข้อมูลที่ไม่มีใน memory

---

## 📚 เอกสารเพิ่มเติม

- ✅ Test scenarios: `TEST_SCENARIOS.txt`
- ✅ OpenAPI spec: `openapi.json`
- ✅ README: `README.md`

---

## 🔗 Service URLs

- **API Endpoint:** `http://localhost:8000`
- **Qdrant Dashboard:** `http://localhost:6333/dashboard`
- **Ngrok Public URL:** ดูจาก `./start.sh` output

---

## 🛠️ Management Commands

```bash
# เริ่ม services
./start.sh

# ดู status
./status.sh

# ดู logs
./logs.sh

# ทดสอบ API
./test-http.sh

# หยุด services
./stop.sh

# Restart services
./restart.sh
```

---

## ✅ Checklist ก่อนใช้งานจริง

- [ ] Import `openapi.json` เข้า Dify แล้ว
- [ ] เพิ่ม tools ทั้ง 3 ตัวแล้ว (add, search, getAll)
- [ ] ใส่ instructions แล้ว
- [ ] ทดสอบ Test Case 1-6 แล้ว
- [ ] ตรวจสอบ logs ว่า tools ถูกเรียกใช้ถูกต้อง
- [ ] Agent ตอบกลับเป็นภาษาธรรมชาติ (ไม่ใช่ JSON)
- [ ] Memory ถูกบันทึกและค้นหาได้ถูกต้อง

---

🎉 **ขอให้ใช้งานสนุกครับ!**

# 🧪 Test Scenarios สำหรับทดสอบ Agent ใน Dify

## Test Case 1: บันทึกข้อมูลโปรไฟล์ ✅

**คุณพิมพ์:**
```
สวัสดีครับ ผมชื่อ จอห์น อายุ 28 ปี เป็นโปรแกรมเมอร์ อยู่กรุงเทพฯ
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `addMemory` เพื่อบันทึกข้อมูล
- ✅ ตอบกลับแบบเป็นมิตร เช่น "ยินดีที่ได้รู้จักครับคุณจอห์น โปรแกรมเมอร์ในกรุงเทพฯ อายุ 28 ปี"
- ✅ **ไม่** ตอบเป็น JSON

---

## Test Case 2: บันทึกความชอบ ✅

**คุณพิมพ์:**
```
ผมชอบเขียน Python และ FastAPI นะ แต่ไม่ชอบ syntax ที่ยาวเกินไป
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `addMemory` บันทึก preferences
- ✅ ตอบกลับ เช่น "จดไว้แล้วครับ คุณชอบ Python กับ FastAPI และไม่ชอบ syntax ที่ยาว"

---

## Test Case 3: บันทึกเป้าหมาย ✅

**คุณพิมพ์:**
```
เป้าหมายของผมคืออยากเรียนรู้ AI และสร้าง chatbot ด้วย Dify
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `addMemory` บันทึกเป้าหมาย
- ✅ ตอบกลับ เช่น "เยี่ยมเลยครับ เป้าหมายที่ดีมาก ผมจะช่วยคุณให้ถึงเป้าหมายนี้"

---

## Test Case 4: บันทึกงานอดิเรก ✅

**คุณพิมพ์:**
```
ผมชอบดู Formula 1 นะ เป็นแฟนทีม Red Bull Racing
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `addMemory` บันทึกงานอดิเรก
- ✅ ตอบกลับ เช่น "เท่ห์ดี ผมจดไว้แล้วว่าคุณชอบ F1 และเป็นแฟน Red Bull Racing"

---

## Test Case 5: ค้นหาข้อมูล - ชื่อ 🔍

**คุณพิมพ์:**
```
ผมชื่ออะไรนะ
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `searchMemories` ด้วย query เช่น "name" หรือ "ชื่อ"
- ✅ ตอบกลับ "คุณชื่อจอห์นครับ"
- ⚠️ **ไม่** ตอบว่า "ไม่รู้" หรือ "คุณไม่ได้บอก"

---

## Test Case 6: ค้นหาข้อมูล - อาชีพ 🔍

**คุณพิมพ์:**
```
ผมทำงานอะไรนะ
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `searchMemories` ด้วย query เช่น "job" หรือ "อาชีพ"
- ✅ ตอบกลับ "คุณเป็นโปรแกรมเมอร์ครับ"

---

## Test Case 7: ค้นหาข้อมูล - ความชอบ 🔍

**คุณพิมพ์:**
```
ผมชอบภาษาโปรแกรมอะไรนะ
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `searchMemories` ด้วย query เช่น "programming language" หรือ "Python"
- ✅ ตอบกลับ "คุณชอบ Python และ FastAPI ครับ และไม่ชอบ syntax ที่ยาวเกินไป"

---

## Test Case 8: ค้นหาข้อมูลที่ไม่มี ❌

**คุณพิมพ์:**
```
ผมเคยบอกสีโปรดมั้ย
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `searchMemories` ด้วย query เช่น "favorite color" หรือ "สีโปรด"
- ✅ ตอบกลับ "คุณยังไม่ได้บอกสีโปรดครับ" หรือ "ผมไม่มีข้อมูลเกี่ยวกับสีโปรดของคุณในความจำ"
- ⚠️ **ไม่** แต่งคำตอบ หรือเดาว่าคุณชอบสีอะไร

---

## Test Case 9: ดูข้อมูลทั้งหมด 📋

**คุณพิมพ์:**
```
คุณจำอะไรเกี่ยวกับผมได้บ้าง
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `getAllMemories`
- ✅ แสดงสรุปข้อมูลทั้งหมดในรูปแบบที่อ่านง่าย เช่น:
  ```
  ผมจำเกี่ยวกับคุณได้ดังนี้ครับ:
  
  👤 ข้อมูลส่วนตัว:
  - ชื่อ: จอห์น
  - อายุ: 28 ปี
  - อาชีพ: โปรแกรมเมอร์
  - ที่อยู่: กรุงเทพฯ
  
  💻 ความชอบในการเขียนโค้ด:
  - ชอบ: Python, FastAPI
  - ไม่ชอบ: syntax ที่ยาวเกินไป
  
  🎯 เป้าหมาย:
  - เรียนรู้ AI และ machine learning
  - สร้าง chatbot ด้วย Dify
  
  🏎️ งานอดิเรก:
  - ดู Formula 1 racing
  - เป็นแฟนทีม Red Bull Racing
  ```

---

## Test Case 10: การสนทนาที่ใช้ memory ในบริบท 💡

**คุณพิมพ์:**
```
ช่วยแนะนำ framework ที่เหมาะกับผมหน่อย
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `searchMemories` หา preferences เกี่ยวกับ programming
- ✅ ตอบกลับโดยอ้างอิงข้อมูลที่จำได้ เช่น:
  ```
  จากที่คุณบอกว่าชอบ Python และ FastAPI ผมขอแนะนำ framework ที่เหมาะกับคุณดังนี้ครับ:
  
  1. FastAPI (ที่คุณใช้อยู่แล้ว) - เหมาะมากสำหรับ REST API
  2. Django - ถ้าต้องการสร้าง full-stack application
  3. Streamlit - สำหรับสร้าง data science apps ง่ายๆ
  
  เนื่องจากคุณไม่ชอบ syntax ที่ยาว ผมแนะนำให้ใช้ FastAPI หรือ Streamlit เพราะมี syntax ที่กระชับและอ่านง่าย
  ```

---

## Test Case 11: อัปเดตข้อมูล (หากใช้งานต่อ) 🔄

**คุณพิมพ์:**
```
อ้อ ตอนนี้ผมอายุ 29 แล้ว วันเกิดผ่านไปเมื่อเดือนที่แล้ว
```

**คาดหวัง:**
- ✅ Agent เรียกใช้ `addMemory` หรือ `updateMemory` (ถ้ามี)
- ✅ ตอบกลับ "สุขสันต์วันเกิดย้อนหลังครับ! ผมอัปเดตข้อมูลแล้วว่าตอนนี้คุณอายุ 29 ปี"

---

## Test Case 12: ไม่บันทึกคำถามทั่วไป ⚠️

**คุณพิมพ์:**
```
อากาศวันนี้ดีมั้ย
```

**คาดหวัง:**
- ⚠️ Agent **ไม่** เรียกใช้ `addMemory`
- ✅ ตอบคำถามทั่วไปตามปกติ
- เหตุผล: นี่ไม่ใช่ข้อมูลส่วนตัวที่ต้องบันทึก

---

## Test Case 13: ไม่บันทึกการทักทาย ⚠️

**คุณพิมพ์:**
```
สวัสดีครับ
```

**คาดหวัง:**
- ⚠️ Agent **ไม่** เรียกใช้ `addMemory`
- ✅ ตอบกลับทักทายตามปกติ เช่น "สวัสดีครับ มีอะไรให้ช่วยไหมครับ"
- เหตุผล: การทักทายไม่ใช่ข้อมูลที่ต้องบันทึก

---

## 📊 สรุป Test Results

| Test Case | Tool ที่ใช้ | Status |
|-----------|------------|--------|
| 1. บันทึกโปรไฟล์ | addMemory | ⬜ |
| 2. บันทึกความชอบ | addMemory | ⬜ |
| 3. บันทึกเป้าหมาย | addMemory | ⬜ |
| 4. บันทึกงานอดิเรก | addMemory | ⬜ |
| 5. ค้นหาชื่อ | searchMemories | ⬜ |
| 6. ค้นหาอาชีพ | searchMemories | ⬜ |
| 7. ค้นหาความชอบ | searchMemories | ⬜ |
| 8. ค้นหาข้อมูลที่ไม่มี | searchMemories | ⬜ |
| 9. ดูข้อมูลทั้งหมด | getAllMemories | ⬜ |
| 10. ใช้ memory ในบริบท | searchMemories | ⬜ |
| 11. อัปเดตข้อมูล | addMemory | ⬜ |
| 12. ไม่บันทึกคำถามทั่วไป | - | ⬜ |
| 13. ไม่บันทึกการทักทาย | - | ⬜ |

---

## 🔍 วิธีตรวจสอบว่า Test ผ่านหรือไม่

### ใน Dify Dashboard:
1. ไปที่ **Logs & Ann.** tab
2. คลิกที่ conversation ที่ต้องการตรวจสอบ
3. ดู "Tool Calls" section ว่า:
   - ✅ เรียกใช้ tool ที่ถูกต้องหรือไม่
   - ✅ ส่ง parameters ที่ถูกต้องหรือไม่
   - ✅ ได้ response ที่ถูกต้องหรือไม่

### ตัวอย่าง Log ที่ถูกต้อง:
```json
{
  "tool": "addMemory",
  "input": {
    "messages": "Name: John, Age: 28, Job: programmer, Location: Bangkok",
    "user_id": "user_123"
  },
  "output": {
    "status": "success",
    "data": {
      "results": [
        {"id": "...", "memory": "Name is John", "event": "ADD"},
        {"id": "...", "memory": "Age is 28", "event": "ADD"},
        ...
      ]
    }
  }
}
```

---

## ⚠️ ปัญหาที่พบบ่อย

### 1. Agent ตอบเป็น JSON แทนที่จะเป็นภาษาธรรมชาติ
**แก้ไข:** เพิ่มใน instructions: "ตอบเป็นภาษาธรรมชาติ ไม่ใช่ JSON"

### 2. Agent ไม่เรียกใช้ tools
**แก้ไข:** ตรวจสอบว่าใส่ instructions แล้วหรือยัง และ tools ถูก enable หรือไม่

### 3. Agent บันทึก memory มากเกินไป
**แก้ไข:** เพิ่มส่วน "⚠️ ไม่ต้องบันทึก" ใน instructions

### 4. Agent ไม่ค้นหาก่อนตอบ
**แก้ไข:** เพิ่มใน instructions: "ค้นหา memory ก่อนตอบคำถามเกี่ยวกับผู้ใช้"

---

## 🎯 Tips สำหรับการทดสอบ

1. ✅ **ทดสอบทีละ case** - อย่ารีบทดสอบหลาย case พร้อมกัน
2. ✅ **ดู logs หลังทุก test** - เพื่อดูว่า Agent เรียกใช้ tool ถูกต้องหรือไม่
3. ✅ **ใช้ user_id ที่แตกต่างกัน** - สำหรับแต่ละ session เพื่อทดสอบ user isolation
4. ✅ **รอให้ Agent ตอบเสร็จก่อนพิมพ์ต่อ** - ไม่งั้น conversation context อาจจะสับสน
5. ✅ **ทดสอบใหม่หลังแก้ instructions** - เพื่อดูว่าการแก้ไขมีผลหรือไม่

---

✅ **เมื่อผ่าน Test Cases ทั้งหมดแล้ว Agent พร้อมใช้งานจริง!**
